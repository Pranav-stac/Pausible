/** Bundled fallback id for routes and demos; full item bank comes from `/question.json`. */
export const defaultAssessmentId = "default";

export { buildOceanPersonalityAssessment as getDefaultAssessment } from "@/data/ocean-personality-assessment";
